CURRENT="$(find /var/local/wallpapers/spotlight/ -type f | shuf -n10 | shuf -n1)";

if [[ ! -z "$(which swww)" ]]; then 
    swww img ${CURRENT}
    ln -fs "${CURRENT}" /var/local/wallpapers/.current
fi
